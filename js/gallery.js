document.addEventListener('DOMContentLoaded',()=>{const modal=document.querySelector('.modal');document.querySelectorAll('.gallery-item img').forEach(image=>image.addEventListener('click',()=>{if(!modal)return;modal.querySelector('img').src=image.src;modal.querySelector('img').alt=image.alt;modal.classList.add('open')}));modal?.querySelector('button').addEventListener('click',()=>modal.classList.remove('open'));modal?.addEventListener('click',e=>{if(e.target===modal)modal.classList.remove('open')})});
