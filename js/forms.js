document.addEventListener('DOMContentLoaded',()=>{const form=document.querySelector('[data-contact-form]');if(!form)return;form.addEventListener('submit',event=>{event.preventDefault();const message=form.querySelector('[name=message]'),email=form.querySelector('[name=email]'),output=form.querySelector('.form-message');if(!form.checkValidity()||message.value.trim().length<20){output.textContent='Please complete all required fields and enter a message of at least 20 characters.';output.style.color='#b91c1c';return}output.textContent='Thank you. This prototype has validated your message; backend/email integration is still required before it can be delivered.';output.style.color='#008751';form.reset()})});
